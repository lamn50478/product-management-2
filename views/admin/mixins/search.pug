mixin search(keyword)
  form(id="form-search")
    .input-group
      input(
        type="text"
        placeholder="Nhap tu khoa"
        name="keyword"
        class="form-control"
        value=keyword
      )
      button(
        class="btn btn-success"
        type="submit"
      ) Tim kiem
